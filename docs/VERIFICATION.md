# 실데이터 검증 기록

2026-09-24(추석 연휴 첫날) 실제 API 로 전체 파이프라인을 돌리며 발견한 문제와 조치입니다.
각 항목은 재현 → 수정 → 회귀 테스트(또는 측정)로 닫았습니다.

| # | 발견 | 원인 | 조치 | 확인 |
|---|---|---|---|---|
| 1 | 기획서의 통행시간 경로 `upDownTrafficTime` 이 404 | 예제 목록과 실제 서비스 경로 불일치 (U-2) | `trtm/realUnitTrtm` 사용, 응답 구조(하루 전체·차종 정렬·99행)에 맞춰 꼬리 커서 설계 | 계약 테스트 `test_ex_travel_time` |
| 2 | 서울→대전 같은 장거리 영업소 쌍의 값이 100~400분 | 5분 슬롯당 표본 1~6대, 휴게소 정차 차량이 평균을 부풀림 | 길 = 짧은 인접 구간 체인의 합 (ADR-008) | 서울→천안 상행 합 52~55분 ↔ 카카오 55분 |
| 3 | 인접 영업소 쌍 중 데이터가 없는 쌍 (기흥→오산), 체인이 옆 노선으로 샘(서울–강릉 UP 이 속초 경유) | `updownIcList` 는 코드 순 · 노선 전체를 후보로 넣음 | `tools/build_seed.py`: 좌표 투영 순서 + 행 수 확인 + 백트래킹 + 길마다 후보 제한 | 8개 길 체인 · 방향별 거리 대칭 확인 |
| 4 | 하행 전 구간이 상행의 2~3배 (서울→대전 하행 평균 3시간 38분) | **실제 추석 귀성길 정체** — 가장 빠른 차량(timeMin)도 느렸음 | 데이터는 정상. 그러나 품질 규칙 Q-v1(속도<40 ∧ 차량<5)이 이 정체의 1,132행을 SUSPECT 로 잘못 거름 → Q-v2(흐르는 차와 멈춘 차가 섞인 경우만) + 튀는 값 필터 H-v1 로 교체, `roadrail reclassify-road` 로 재분류 | SUSPECT 1,132 → 44행. `test_real_congestion_even_fastest_is_slow_is_ok` |
| 5 | 에어코리아 작업 FAILED (`NoneType.endswith`) | 점검 중 측정소 행의 `dataTime` 이 null | 파서에서 해당 행 건너뜀 | 회귀 테스트 `test_airkorea` |
| 6 | 판단 카드 500 | Spring 7 `JdbcClient.single()` 이 `max()` 결과 null 에서 예외 | null 가능 조회를 `optional()` 로 | `ApiIT.nowCardHasEvidenceFreshnessAndCaches` |
| 7 | 관리 API 가 예산 초과를 429 로 막지 못함 | `DateTimeFormatter.BASIC_ISO_DATE` 가 `OffsetDateTime` 에 `+0900` 을 붙여 Redis 키(`quota:KORAIL:20260924+0900`)가 collector 와 불일치 | `yyyyMMdd` 패턴 | `ApiIT.adminRequiresTokenAndChecksLockAndBudget` |
| 8 | 첫날 "같은 요일·시간 중앙값 대비 0%" | 기준선이 오늘 하루(=자기 자신)로만 만들어짐 | FR-402 대로 표본 n<4 이면 비교하지 않고 근거에 '비교 불가' 명시 | 판단 근거 문구 |
| 9 | 판단 카드 캐시 미적중 1.57초 (NFR-03 목표 1초 초과) | 카카오 미래 운행 정보 응답이 약 1초 | 카카오 호출을 가상 스레드로 비동기 실행하고 최대 0.6초만 대기, 나머지는 캐시로 (ADR-010) | 미적중 0.52~0.82초 · 적중 7~9ms |
| 10 | 수집 상태의 완전성이 첫날 4%, 공개 지연 624분 | 수집 시작 전 시간까지 분모에 포함, 첫 대량 수집·값 갱신 행을 지연으로 계산 | 창 시작 = max(24h 전, 첫 수집), `collected_at` 은 처음 저장 시각으로 고정 | `/ops` 화면 |
| 11 | 히어로 지도가 제목·버튼을 덮음, 배율이 한반도 전체 | 카카오 SDK 가 컨테이너를 `position: relative` 로 바꾸고 내부 z-index 가 형제 위로 올라옴. 백그라운드 탭에서 rAF 정지 | 바깥 래퍼 + `isolation: isolate`, 타이머로 `relayout → setBounds` | Playwright 스크린샷 |
| 12 | (코드 검토) 헬스 체크마다 Redis 연결이 샘 | `getConnection()` 으로 연 연결을 닫지 않음 (10초마다 헬스 체크) | `execute(RedisCallback)` 으로 빌려 쓰고 반납 | 코드 검토 |
| 13 | (코드 검토) 관리 명령이 실행 도중 재기동되면 사라짐 | 작업을 띄우자마자 XACK — 문서의 '처리 후 XACK' 와 불일치 | 작업이 끝난 뒤 XACK, 미처리(pending)는 기동 시 한 번만 재처리 (매번 읽으면 실행 중 명령이 중복 실행) | 코드 검토 |
| 14 | 심야 출발이면 "직통 열차 없음" | 그날 남은 열차만 찾음 | 다음 날 첫차까지 찾아 대기 시간에 포함 | 23:40 서울역→대전역 |
| 15 | 역 좌표 258/276 (93%) | 카카오 분류가 '전철역'인 경춘·경원선 역, 괄호 역명(판교(경기)·판교(충남)), '진부(오대산)역' | 기차역 → 전철역 순 · 괄호·공백 정규화 · 지역 괄호로 주소 구분 · 역명 접두 일치 | 276/276 · `test_stations.py` 5개 |
| 16 | 출발지→도착지 첫 응답 3.3초 | 카카오·기상청·에어코리아·행정구역 호출을 차례로 기다림 | 병렬 실행 + 하나의 마감(0.75초) 공유, 늦은 값은 pending (ADR-013) | 0.69~0.77초 |
| 17 | (코드 검토) 단기예보 응답에 항목 필드가 없으면 NPE | `"…".contains(null)` | null 방어 | 코드 검토 |
| 18 | 역 선택 목록 맨 아래(수원역 등)가 잘림 | 히어로 영역의 `overflow-hidden` 이 절대 위치 목록을 잘라냄 | 히어로 `overflow-hidden` 제거 · 목록 높이 min(22rem, 60vh) | E2E '선택 목록: 맨 아래 항목까지 잘리지 않는다' |
| 19 | 페이지를 스크롤하면 지도 배율이 바뀜 | 탐색용 지도의 휠 확대가 켜져 있음 | 기본 잠금(이동·휠 확대 끔) + +/− 버튼 + '지도 조작하기' 버튼, 지도 밖으로 나가면 다시 잠금 | E2E '지도: 기본은 잠김' |
| 20 | 전주→부산 등 직통이 없는 조합은 비교 불가 | 직통 열차만 탐색 | CSA 환승 경로 (ADR-014) | 전주역 → 오송 환승 → 부산역 |
| 21 | 판단 지도의 기차 경로가 역과 역을 잇는 직선 | 코레일 API 에 선로 좌표가 없음 | OpenStreetMap 선로 → 역 쌍별 최단 선로(`ref.rail_link`), 열차가 실제로 선 역 순서대로 이어 그림 (ADR-015) | 역 쌍 1,056/1,056 · 직선 대비 중앙값 1.12배 · `test_rail_graph.py` · `ApiIT`(legs[].path) |
| 22 | Overpass 406 · 504, 서울→용산 86km | User-Agent 없음 · 전국 한 번 요청은 시간 초과 · `service` 필터가 분기선을 빼서 그래프 끊김 · 역을 한 점에만 붙여 반대 본선으로 돎 | UA 헤더 · 4구역 분할 + 미러 순환 · 필터 제거 · 역을 반경 400m 안 모든 선로 점에 연결 | 서울→용산 2.9km · 서울→대전 160km(직선 142km) |
| 23 | 첫 `rail_geometry` 실행 FAILED `KeyError: 'OSM'` — **새 '오류 상세' 영역에서 스택 트레이스로 바로 확인** | 공급자를 추가하면서 일일 예산 설정을 빠뜨림 | `quota_osm`(60) · 모든 공급자에 예산이 있는지 검사하는 테스트 | `test_every_provider_has_a_daily_quota` |
| 24 | 에어코리아 부분 실패의 오류 메시지가 빈 문자열 (`PARTIAL: 강원 …: `) | httpx 시간 초과 예외는 `str(e)` 가 비어 있고, 오류 없는 호출로 보여 '실패한 외부 호출'에서도 빠짐 | 예외 이름을 함께 기록 (`ReadTimeout: …`) | `test_timeout_is_named_and_shows_in_run_detail` |
| 25 | (개발 중) 역 검색 500 | 정렬 옵션을 넣으며 SQL 에 `String.formatted()` 를 썼는데 `LIKE '%'` 가 서식 문자로 해석됨 | 자리표시자 치환으로 | `ApiIT.railOdAndStations`(가나다순 · 잘못된 sort 400) |
| 26 | 복사 버튼의 접근성 이름이 비어 있음 | 버튼 안에 `role="status"` 를 넣어 이름 계산에서 빠짐 | 알림 영역을 버튼 밖(sr-only)으로 | E2E '수집 상태: 오류 상세와 복사' |
| 27 | 수집 상태에 끝난 작업이 '실행 중'으로 남음 (kakao_eta · road_travel_time · rail_geometry), kakao_eta 가 30분 동안 건너뜀 | 재배포로 수집기가 멈출 때 ① 취소된 작업이 상태 'OK' 인 채 마무리되려다 ② DB 연결이 먼저 닫혀 기록 실패 → `RUNNING` · 잠금 · 쓰지 않은 예약이 남음 | 취소 시 '중단'으로 기록 · 종료 때 작업 마무리를 7초 기다림 · 기동 시 남은 RUNNING/잠금/예약 정리 | `test_cancelled_job_is_recorded_as_aborted_and_unlocked` · `test_restart_recovers_stale_runs_locks_and_reservations` |
| 28 | 공개 Overpass 가 몹시 느린 날 선로 작업이 23분 뒤 실패 (작은 조회도 60초, 504) | 외부 서비스 상태 | 받은 구역 Redis 3일 캐시(다시 실행하면 못 받은 구역만) · 미리 받은 파일로 계산하는 `roadrail rail-geometry FILE` · 오류 메시지 앞머리 중복 제거 | 오류 상세에 실패한 호출 10건이 그대로 보임 → 파일로 1,056쌍 계산 (2.3초) |

## 알려진 한계 (수치로 확인됨)

- **중간역 지연 보간(P-i1)은 지연을 과소 추정하는 경향.** 종착역 정확 비교(EXACT)가 대부분인 방향의 정시율(서울→부산 66.7%, 동대구→부산 68.9%)이 보간(EST)이 대부분인 방향(부산→동대구 97.7%, 서울→천안아산 96.8%)보다 낮다. 지연은 종착역 부근에서 비선형으로 쌓이는데 선형 보간이 이를 반영하지 못하는 것으로 보인다. 화면에 ⚠ 추정과 추정 비율을 표시한다. 개선 후보: 역별 표준 운행시간(최빈값) 기반 계획 시각 추정.
- 기차 환승은 **코레일 여객열차끼리만** 다룬다. 지하철·버스 환승 경로는 공개 데이터로 만들 수 없어(TAGO 는 역별 시각만) 다루지 않는다. 역까지 도보는 1km 미만만 추정한다.
- 도로 종류는 이름으로 분류하므로 도로명으로 표기된 국도 구간은 일반도로로 집계된다.
- 고속도로 **실측** 추이(평소 대비 정체)는 수집 중인 8개 길에서만 제공한다. 그 밖은 카카오 경로 예측이다.
- **도로공사 공개 지연(수 시간)** 때문에 "지금" 도로 값은 몇 시간 전 관측을 M1 로 끌어온 예측이다 (`leadMin` 표시). 자정 직후 전날 마지막 몇 시간 슬롯은 API 가 날짜를 넘겨 버려 받을 수 없다 (결측 사유 `SOURCE_EXPIRED`).
- 기준선·백테스트는 데이터가 쌓여야 의미가 있다. 표본 수를 항상 함께 표시하고, 부족하면 '—' 로 표시한다.
